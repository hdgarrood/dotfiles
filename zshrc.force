# Like .zshrc, but sourcing this will do the lot even if it's a non-interactive
# shell.

ZSH_CONFIG_FILES=~/.zsh/*.zsh

for config_file in $(ls $ZSH_CONFIG_FILES); do
    source "$config_file"
done

if [ -f ~/.zshrc.local ]; then
    source ~/.zshrc.local
fi
